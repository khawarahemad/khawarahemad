<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 800 800" width="100%" height="100%">
  <foreignObject x="0" y="0" width="100%" height="100%">
    <div xmlns="http://www.w3.org/1999/xhtml" style="width: 100%; height: 100%; overflow: hidden; margin: 0; padding: 0;">
      <style type="text/css">
        /* <![CDATA[ */
body{
  margin:0;
  min-height:100vh;
}

.glass-container {
  position: relative;
  width: 100%;
  min-height: 100vh;
  background: #090a10;
  display: flex;
  align-items: center;
  justify-content: center;
  overflow: hidden;
  font-family: Inter, sans-serif;
}

.glow-bg {
  position: absolute;
  width: 350px;
  height: 350px;
  background: radial-gradient(
    circle,
    rgba(139,92,246,.5),
    transparent 70%
  );
  animation: floatBlur 6s ease-in-out infinite;
}

@keyframes floatBlur {
  0%,100%{
    transform:translate(0,0);
  }
  50%{
    transform:translate(60px,30px);
  }
}

.card-content {
  position: relative;
  z-index: 2;

  width: 650px;
  max-width: 95%;

  background: rgba(255,255,255,0.04);

  border: 1px solid rgba(255,255,255,0.08);

  backdrop-filter: blur(20px);
  -webkit-backdrop-filter: blur(20px);

  border-radius: 24px;

  padding: 30px;

  display: flex;
  align-items: center;
  gap: 25px;

  box-shadow:
    0 0 40px rgba(139,92,246,.2);

  transition:.4s;
}

.card-content:hover{
  transform:translateY(-8px);
}

.avatar-ring {
  position: relative;

  width: 120px;
  height: 120px;

  border-radius: 50%;

  padding: 4px;

  background:
    linear-gradient(
      135deg,
      #00f5ff,
      #8b5cf6
    );

  animation: rotateGlow 8s linear infinite;
}

@keyframes rotateGlow{
  from{
    transform:rotate(0deg);
  }
  to{
    transform:rotate(360deg);
  }
}

.avatar {
  width:100%;
  height:100%;
  border-radius:50%;
  object-fit:cover;
  background:#111;
}

.active-badge {
  position:absolute;
  bottom:8px;
  right:8px;

  width:16px;
  height:16px;

  border-radius:50%;
  background:#10b981;

  box-shadow:
    0 0 12px #10b981;
}

.details{
  flex:1;
  color:white;
}

.details h2{
  margin:0;
  font-size:32px;
  font-weight:800;
}

.subtitle{
  color:#94a3b8;
  margin-top:8px;
  margin-bottom:18px;
}

.tags{
  display:flex;
  flex-wrap:wrap;
  gap:10px;
}

.tags span{
  background:rgba(139,92,246,.15);
  color:#c084fc;

  border:1px solid rgba(139,92,246,.3);

  border-radius:8px;

  padding:6px 12px;

  font-size:12px;
  font-weight:600;

  transition:.3s;
}

.tags span:hover{
  transform:translateY(-3px);
  background:#8b5cf6;
  color:white;
}

.socials{
  margin-top:20px;
  display:flex;
  gap:12px;
}

.socials a{
  text-decoration:none;
  color:white;

  padding:10px 18px;

  border-radius:10px;

  background:
    linear-gradient(
      135deg,
      #00f5ff,
      #8b5cf6
    );

  transition:.3s;
}

.socials a:hover{
  transform:translateY(-3px);
  box-shadow:
    0 10px 25px rgba(139,92,246,.4);
}
        /* ]]> */
      </style>
<div class="glass-container">
  <div class="glow-bg"></div>

  <div class="card-content">

    <div class="avatar-ring">
      <img src="https://github.com/khawarahemad.png" alt="Khawar Ahemad Khan" class="avatar" />
      <div class="active-badge"></div>
    </div>

    <div class="details">
      <h2>Khawar Ahemad Khan</h2>

      <p class="subtitle">
        Full Stack Developer • Cyber Security • AI Enthusiast
      </p>

      <div class="tags">
        <span>#nextjs</span>
        <span>#react</span>
        <span>#python</span>
        <span>#ai</span>
        <span>#security</span>
      </div>

      <div class="socials">
        <a href="https://github.com/khawarahemad" target="_blank">
          GitHub
        </a>

        <a href="https://khawarahemad.com" target="_blank">
          Portfolio
        </a>
      </div>
    </div>

  </div>
</div>
    </div>
  </foreignObject>
</svg>
